class CSearchOptions:

    def __init__(self, word, fuzzyness, onlyTitle, onlyOcr):
        self.word = word
        self.fuzzyness = fuzzyness
        self.onlyTitle = onlyTitle
        self.onlyOcr = onlyOcr


